name = "inforhealth"
compatibility_date = "2024-06-01"

# Site estático: build gera ./dist (build.sh ou npm run build)
[assets]
directory = "dist"
html_handling = "auto-trailing-slash"
not_found_handling = "404-page"
