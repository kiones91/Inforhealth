{
  "name": "inforhealth-portal",
  "private": true,
  "scripts": {
    "build": "node tools/deploy-build.mjs",
    "build:ci": "node tools/deploy-build.mjs",
    "deploy": "npm run build -- --force && wrangler deploy",
    "deploy:ci": "npm run build -- --force && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.98.0"
  }
}
