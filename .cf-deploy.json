{
  "build_command": "",
  "deploy_command": "npm ci && npx wrangler deploy",
  "comment": "Painel: Workers > inforhealth > Settings > Build. Deixe Build vazio; dist/ ja esta no Git (317 arquivos). O token do Git da Cloudflare faz o deploy — nao precisa de API token manual."
}
