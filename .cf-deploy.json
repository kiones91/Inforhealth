{
  "build_command": "npm install && npm run build:ci",
  "deploy_command": "npx wrangler deploy",
  "comment": "dist/ versionado no Git. build:ci preserva dist/ se ja existir. Ajuste estes valores em Workers > inforhealth > Settings > Build."
}
