{
  "build_command": "",
  "deploy_command": "npm ci && npm run build -- --force && npx wrangler deploy",
  "production_branch": "main",
  "root_directory": "/",
  "custom_domain": "inforhealth.buffallos.com.br",
  "dns_record": {
    "type": "AAAA",
    "name": "inforhealth",
    "content": "100::",
    "proxied": true
  },
  "comment": "Workers > inforhealth > Settings > Build. Build vazio; Deploy: npm ci && npm run build -- --force && npx wrangler deploy. DNS AAAA 100:: antes de criar o Worker. Ver docs/DEPLOY-CLOUDFLARE.md"
}
